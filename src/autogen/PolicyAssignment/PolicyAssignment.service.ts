/**
 * This file generated by Apso.
 */
import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { TypeOrmCrudService } from '@nestjsx/crud-typeorm';
import { QueryOptions } from '@nestjsx/crud';
import { ParsedRequestParams } from '@nestjsx/crud-request';

import { PolicyAssignment } from './PolicyAssignment.entity';

@Injectable()
export class PolicyAssignmentService extends TypeOrmCrudService<PolicyAssignment> {
  constructor(@InjectRepository(PolicyAssignment) repo) {
    super(repo);
  }

  /**
   *
   * Solves https://github.com/nestjsx/crud/issues/777
   */
  getSelect(query: ParsedRequestParams, options: QueryOptions) {
    return [...new Set(super.getSelect(query, options))];
  }
}
